import { GisMapShell } from "../components/gis-map-shell";
export function LayerCataloguePage() {
  return (
    <GisMapShell
      title="Layer catalogue"
      description="Typed layers, capability requirements, provenance, visibility, and bounded feature limits."
      compact
    />
  );
}
