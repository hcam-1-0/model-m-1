import { GisMapShell } from "../components/gis-map-shell";
export function OverviewPage() {
  return (
    <GisMapShell
      title="GIS overview"
      description="One generated spatial domain shared with Command Center, with adaptive rendering and an authoritative table."
      compact
    />
  );
}
