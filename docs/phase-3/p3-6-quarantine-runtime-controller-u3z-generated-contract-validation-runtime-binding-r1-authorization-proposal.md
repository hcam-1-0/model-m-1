# P3.6 U3Z Controller R1 Generated Contract-Validation Runtime-Binding R1 Authorization Proposal

## Status

- This is a non-effective planning package only.
- Exact owner authorization is pending.
- No runtime, parent, module, hardware, or machine path was observed.
- No PowerShell parsing, import, dot-sourcing, or execution occurred.
- No generated-validation attempt, retry, U3K action, commit, push, or remote Git action is authorized.

## Preparation Authority

Exact
`D-P3.6-U3Z-CONTROLLER-R1-GENERATED-CONTRACT-VALIDATION-HARNESS-IMPLEMENTATION-ACCEPTANCE`
is recorded in acceptance record SHA-256
`38B89E53B46F921663879DE0614AF318EDF9B66AB36120663D14F61070AD8477`.
It accepts package SHA-256
`47344ECB697AF05C361D52B3CF05DE6E730B8AF4FC1E91C3AAD682FA514727AF`
and permits preparation of this separate proposal only.

## Exact Inputs

- Generated-validation harness:
  `D9EE5CC7599AACCE3363CE5C29EE479D777F94CF886AE779390B7027C40FA382`
- Materialized 288-vector manifest:
  `7BCDFCA583644BD4ED5F4B747C3969B4C9FF4029B48734F8BD4D90BA5D600A21`
- Controller R1 PowerShell source:
  `787655BAC55DDF563E9D1DC43EC37F010C271AB381E541B0734028E3F2B90B31`
- Controller R1 contract:
  `637C5400122874149D9835CC6BC521E5EEC9160222F4A7AC5CA1F6CD99E1BCC4`
- Controller R1 source-vector manifest:
  `D8EDF5C0255B40FB6C28BB014C09DC503D2B53665E69C5AA5AB65C744AEA81E4`
- Machine-disabled Python reference, identity binding only:
  `B5C7328CD666E0A8988F9B616C5B2A914D690A40BF2EA289C1F1C755E42B86F0`

All six files remain immutable. The Python reference is not executed during the
proposed attempt.

## Proposed Future Attempt

The future decision is
`D-P3.6-U3Z-CONTROLLER-R1-GENERATED-CONTRACT-VALIDATION-RUNTIME-BINDING-R1-AUTH`.
It would authorize at most one attempt on `LAB-LAPTOP-01` within 24 hours.
Failure consumes the authorization; no automatic retry is permitted.

The only runtime candidate is `C:\Program Files\PowerShell\7\pwsh.exe` and its
three fixed parent components. Before invoking it, the attempt must establish
canonical, regular, non-reparse, bounded, SHA-256, bounded-version, and
cache-only trust bindings without network retrieval. Alternate runtime, module,
PATH, registry, WMI, package, directory, or hardware discovery is prohibited.

The exact runtime may then be invoked once with `NoLogo`, `NoProfile`, and
`NonInteractive`. It may run only the hash-bound U3Z harness with the exact
harness and materialized-vector hash parameters. The harness may dot-source
only the exact controller R1 source and execute exactly 288 generated-only
`Policy` cases.

The entire process is limited to 120 seconds, 16 KiB stdout, zero stderr, and a
32 KiB parsed result. Timeout terminates the process tree. No environment proxy
or module path is inherited, and module autoloading remains disabled.

## Result And Evidence Policy

Success requires all 288 cases loaded, executed, and passed; zero failed cases;
exact null success projection; all failure action/reason mappings; zero
cross-language projection differences; zero raw retention; zero machine,
network, and retry actions; and stable runtime, parent, and source identities.

Failure is reduced to one of five bounded reason codes: `binding_failed`,
`manifest_failed`, `controller_load_failed`, `validation_failed`, or
`result_serialization_failed`. Success uses
`generated_contract_validation_passed`. Raw stdout, stderr, exceptions,
fixtures, invocation details, environment, identity, and security material are
discarded.

Success or failure may create only the three exact bounded sanitized
authorization, result, and evidence records. Any result requires separate owner
evidence acceptance. A successful attempt alone does not authorize U3K.

## Hard Boundary

This proposal does not authorize PowerShell parsing, import, dot-sourcing, or
execution; Python execution or machine fallback; runtime, manifest, hardware,
machine, storage, F:, B:, ACL, probe, cleanup, Defender, or scanner action;
network or trust retrieval; downloads; artifacts; models; inference; cameras;
media; private or Government data; containers; Kubernetes; profile activation;
deployment; U3K; commit; push; or remote Git.
