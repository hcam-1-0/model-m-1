# P3.5 W9 Narrow Closure Authorization

Status: effective for Option A only; W10 acceptance remains unauthorized.

Decision: `D-P3.5-W9-START: A`.

Authorized by: `mayank-admin` at `2026-08-27T13:49:46Z`.

Machine record:
[`p3-5-w9-start-authorization.json`](../../contracts/phase-3/p3-5-w9-start-authorization.json).

## Immutable Binding

- authorization repository head:
  `6d546fbe1a074e4090fa6d006a67421a27746afe`;
- reviewed W9 planning package digest:
  `9BCC9E9C068E03E94E5461AABDE3B50A4766498406643EE253AF66ECAD8A9B7B`;
- W9 scope proposal SHA-256:
  `62B711EAF2C1EDD21CBCD07751D9A30EF6FA61C11E9ECB190CE6614FE67AB796`;
- selected scope: `narrow_generated_only_closure`.

## Authorized Work

The decision authorizes only the exact Option A actions and file allowlist in
the machine record:

- consolidate existing W1-W8 aggregate evidence;
- add a stdlib-only closure/evidence checker;
- run bounded deterministic generated-contract resource stress without OCR,
  model, font, or external-runtime execution;
- build and inspect local wheel and sdist archives offline;
- retain only aggregate package counts and cryptographic digests;
- prove zero retention, prohibited-payload absence, default-off behavior, and
  a local rollback procedure;
- synchronize tests, CI, readiness, documentation, and backlog;
- prepare, but do not accept, the separate W10 clean-source package.

The exact path allowlist contains only the W9 tool, test, evidence,
authorization, closure documents, existing readiness/index files, and Python CI.
No `app/`, migration, dependency, lockfile, container, API, worker, database, or
persistent-storage path is authorized.

## Continuing Blocks

Network actions remain an empty allowlist. Option A cannot access the external
`E:` model/runtime cache or `B:` RaiDrive, execute a model/font/Tesseract path,
open the frozen final-test split, select thresholds, promote a model, process
cameras/media or real/public/private/Government/police data, retain OCR values or
identifiers, add operational lookup/alert/action behavior, deploy, start P3.6,
or perform remote Git operations.

This is an implementation-start record, not P3.5 acceptance. W10 requires a
new clean-source evidence package and explicit owner decision.
