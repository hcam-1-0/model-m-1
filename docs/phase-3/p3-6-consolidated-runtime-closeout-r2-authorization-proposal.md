# P3.6 Consolidated Runtime Closeout R2 Authorization Proposal

Status: non-effective package preparation only. Owner authorization is pending.

## Why R2 Exists

The consumed R1 attempt passed package, runtime, trust, fixed-parent, and source identity gates, then stopped closed at the first generated-validation process with sanitized reason `process_output_bounds_failed`. No handler validation, U3K, `F:` access, storage action, or closeout occurred.

U4D adds a hash-bound child harness and a typed parent classifier. The child can emit only one UTF-8 JSON line of at most 4,096 bytes, with no BOM, no CLIXML, zero stderr, and zero raw output or exception retention. Its source-only behavior is covered by 384 generated classifier vectors and 97 percent branch coverage; it has not been executed.

## Proposed One-Attempt Sequence

1. Verify the exact R2 package, owner statement, source hashes, unused authorization, node, time window, and output absence.
2. Write the bounded authorization record before runtime, manifest, or machine observation.
3. Bind only the exact PowerShell 7 runtime, its three fixed parents, and cache-only trust state.
4. Bind the exact Microsoft.PowerShell.Utility manifest and its bounded declared closure without manifest evaluation, module import, search, or trust retrieval.
5. Verify every exact U4D, controller, vector, handler, runner, adapter, and U3K policy binding.
6. Run one child process for exactly 416 generated outer-controller cases.
7. Apply the typed U4D process-output classifier and immediately discard raw process material.
8. Only after child acceptance, run one Aggregate handler process for 20 runner and 64 pure-handler generated cases.
9. Reverify all runtime, closure, source, package, and vector identities.
10. Only after all 500 cases pass, evaluate U3K preconditions and permit one sealed U3K-A01 through U3K-A10 storage transaction for exact absent root `F:\HCAM-Quarantine`.
11. Write bounded sanitized result and evidence records and run final allowlisted validation.
12. Only on total success, synchronize Phase 3 records and create one local closeout commit without push.

The sequence permits exactly one attempt. Any failure consumes the authorization and permits no automatic or manual retry under the same package.

## Current Gate

This proposal does not authorize PowerShell parsing, import, dot-sourcing, or execution; Python machine access; runtime, manifest, hardware, machine, storage, `F:`, `B:`, ACL, probe, cleanup, scanners, network, trust retrieval, installation, update, downloads, artifacts, models, inference, cameras, media, private or Government data, containers, Kubernetes, profile activation, deployment, U3K, Phase 3 closeout, commit, push, or remote Git.

The final package digest must be presented to `mayank-admin`. Authority cannot be inferred from source-build success, this document, `continue`, silence, or any earlier authorization.
